{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/server/**",
      "dest": "http://$SERVER_URL{request.url}"
    }
  ]
}
